architectury {
    common(["fabric", "neoforge"])
}

dependencies {
    implementation libs.fabric.loader
    implementation project(":common")
    implementation project(path: ":common", configuration: "clientClassesElements")
}
