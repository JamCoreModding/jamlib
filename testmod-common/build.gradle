architectury {
    common(['fabric', 'neoforge'])
}

dependencies {
    implementation libs.fabric.loader
    implementation project(path: ':common', configuration: 'mergedElements')
    implementation libs.jankson
}
